# GitHub Configuration Context

`.github/` contains repository automation configuration. Changes here affect remote validation or deployment and must be reviewed against the root context and deployment architecture. Heavy install, typecheck, lint, test, static build, and Pages deployment work belongs in Actions rather than on the local machine.
