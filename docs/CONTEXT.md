# Documentation Context

`docs/` contains durable architecture decisions and documentation that should not be embedded in application code. Each decision is a small, reviewable Markdown artifact. Read root `AGENTS.md`, `CONTEXT.md`, `REFERENCE.md`, and `PRD.md` before adding documentation.
