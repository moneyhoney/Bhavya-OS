# AI Labs Route Context

This folder owns the `/labs` public entry point. Locations, equipment, schedules, capacity, and access workflows are not established and must remain explicitly unpublished. Read `app/CONTEXT.md`, `PRD.md`, and governance references before modifying it.
