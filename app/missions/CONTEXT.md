# Missions Route Context

This folder owns the `/missions` public route. It describes the four constitution-defined mission areas without inventing projects, locations, metrics, or operational claims. Read `app/CONTEXT.md` and the mission source files before modifying it.
