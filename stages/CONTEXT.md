# Stages Context

`stages/` is the Layer 2 ICM execution boundary for sequential, reviewable repository work. Each numbered stage has one job, explicit inputs/outputs, validation, failure conditions, and a human review point. Stage artifacts must remain readable and versioned.
