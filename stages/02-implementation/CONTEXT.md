# Stage 02: Implementation

## Contract
Implement one bounded product slice. Inputs are approved architecture, PRD requirements, brand assets, and scoped context. Outputs are versioned code, docs, and assets. Run lightweight local checks and defer production builds to Actions. Fail on secrets, fabricated facts, or speculative scope.
